[project]
name = "gymnasium-example"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "gfog",
    "gymnasium>=1.2.0",
    "numpy>=2.3.1",
]

[tool.uv.sources]
gfog = { workspace = true }
